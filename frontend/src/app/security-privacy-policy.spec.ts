import { SecurityPrivacyPolicy } from './security-privacy-policy';

describe('SecurityPrivacyPolicy', () => {
  it('should create an instance', () => {
    expect(new SecurityPrivacyPolicy()).toBeTruthy();
  });
});
